# ============================================================
# GenAI Banking RAG Chatbot — Backend Dependencies
# Python 3.11+  |  FastAPI + ChromaDB + Sentence-Transformers
#
# NOTE: torch is NOT pinned to a specific version.
# pip will automatically select the correct wheel for your
# OS, CPU architecture (x86 / Apple Silicon), and Python version.
# ============================================================

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.1

# --- Environment & Config ---
python-dotenv>=1.0.1
pydantic>=2.7.1
pydantic-settings>=2.3.0

# --- File Parsing ---
pypdf>=4.2.0
python-multipart>=0.0.9

# --- Embeddings (sentence-transformers/all-MiniLM-L6-v2) ---
# torch: let pip pick the right wheel for your platform automatically
torch>=2.0.0
sentence-transformers>=3.0.0
transformers>=4.41.0

# --- Vector Database ---
chromadb>=0.5.3

# --- LLM — Google Gemini (new unified SDK — replaces google-generativeai) ---
google-genai>=1.0.0

# --- LLM Fallback — Groq (free tier, OpenAI-compatible, used if Gemini quota is unavailable) ---
groq>=0.9.0

# --- Utility ---
langchain-text-splitters>=0.2.2
tenacity>=8.3.0
loguru>=0.7.2
